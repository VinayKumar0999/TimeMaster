import {
  RightCircleOutlined
} from '@ant-design/icons';
const Header =()=> {
    return (
        <header className="header">
            <RightCircleOutlined style={{ fontSize: '26px', color: '#08c' }}/>
        </header>
    );
}

export default Header